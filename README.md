# Python application for read .opf files

Python application for read OPF files and organize your personal library in your computer

## Requirements

1. Pyhon 3+ 

## Setup

1. Install the requirments. Run `pip install -r requirements.txt` in the CMD